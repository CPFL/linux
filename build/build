#!/bin/bash

pushd `dirname $0` > /dev/null
SCRIPTPATH=`pwd`
popd > /dev/null
SCRIPTDIR=`dirname $SCRIPTPATH`
echo $SCRIPTDIR

make -C "$SCRIPTDIR" -f "$SCRIPTDIR/build/Makefile" NOUVEAUROOTDIR="$SCRIPTDIR" LINUXDIR="$SCRIPTDIR" $*
